// #pghbnSB
// - створити функцію яка повертає найменьше число з масиву

let minNumb = (array) => {
    let min = array[0];
    for (let item of arr) {
        if (min > item) {
            min = item;
        }
    }
    return console.log(min);
}

let arr = [1, 5, 6, 5, -1];


minNumb(arr);